
{
  "entities": {
    "Budget": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Budget",
      "type": "object",
      "description": "Represents the overall budget for a celebration, including event details.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the budget."
        },
        "name": {
          "type": "string",
          "description": "Name of the plan or budget (e.g., 'Elkay's Wedding Plan')."
        },
        "eventLocation": {
          "type": "string",
          "description": "The location of the event."
        },
        "expectedGuests": {
          "type": "number",
          "description": "The number of expected guests."
        },
        "eventType": {
          "type": "string",
          "description": "The type of event (e.g., Wedding, Birthday, Funeral)."
        },
        "eventDate": {
            "type": "string",
            "format": "date-time",
            "description": "The date of the event."
        },
        "grandTotal": {
          "type": "number",
          "description": "The overall grand total of the budget."
        },
        "userId": {
          "type": "string",
          "description": "Reference to User. (Relationship: User 1:N Budget)"
        },
        "collaboratorIds": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Array of user IDs who have collaborator access to this budget."
        }
      },
      "required": [
        "id",
        "name",
        "grandTotal",
        "userId"
      ]
    },
    "Category": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Category",
      "type": "object",
      "description": "Represents a category within the budget (e.g., 'Venue', 'Catering', 'Decorations').",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the category."
        },
        "name": {
          "type": "string",
          "description": "Name of the category."
        },
        "budgetId": {
          "type": "string",
          "description": "Reference to Budget. (Relationship: Budget 1:N Category)"
        },
        "total": {
          "type": "number",
          "description": "The total cost for this category."
        },
        "order": {
          "type": "number",
          "description": "The display order of the category."
        }
      },
      "required": [
        "id",
        "name",
        "budgetId",
        "total",
        "order"
      ]
    },
    "Item": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Item",
      "type": "object",
      "description": "Represents an individual item within a category (e.g., 'Wedding Cake', 'Flowers', 'DJ').",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the item."
        },
        "name": {
          "type": "string",
          "description": "Name of the item."
        },
        "quantity": {
          "type": "number",
          "description": "Quantity of the item."
        },
        "unitPrice": {
          "type": "number",
          "description": "Price per unit of the item."
        },
        "total": {
          "type": "number",
          "description": "Total cost of the item (quantity * unitPrice)."
        },
        "categoryId": {
          "type": "string",
          "description": "Reference to Category. (Relationship: Category 1:N Item)"
        },
        "comment": {
          "type": "string",
          "description": "Comment on how the item cost was determined."
        }
      },
      "required": [
        "id",
        "name",
        "quantity",
        "unitPrice",
        "total",
        "categoryId"
      ]
    },
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "displayName": {
          "type": "string",
          "description": "Display name of the user."
        },
        "knownAs": {
            "type": "string",
            "description": "The name the user prefers to be called."
        },
        "photoURL": {
            "type": "string",
            "description": "URL of the user's profile picture."
        }
      },
      "required": [
        "id",
        "email"
      ]
    },
    "MustDo": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "MustDo",
      "type": "object",
      "description": "Represents a critical, non-budgeted action item for an event.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the must-do item." },
        "budgetId": { "type": "string", "description": "Reference to the parent Budget." },
        "userId": { "type": "string", "description": "Reference to the owning User." },
        "title": { "type": "string", "description": "A short, action-oriented title for the task." },
        "note": { "type": "string", "description": "Optional context or details about the task." },
        "status": { "type": "string", "enum": ["todo", "done"], "description": "The completion status of the task." },
        "priority": { "type": "string", "enum": ["low", "medium", "high"], "description": "The priority of the task." },
        "deadline": { "type": "string", "format": "date", "description": "The deadline for the task." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of when the item was created." },
        "reminderType": { "type": "string", "enum": ["none", "email", "sms", "whatsapp"], "description": "The type of reminder to send for the task." },
        "reminderDaysBefore": { "type": "number", "description": "How many days before the deadline to send the reminder." }
      },
      "required": ["id", "budgetId", "userId", "title", "status", "priority", "createdAt", "reminderType", "reminderDaysBefore"]
    },
    "RSVP": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "title": "RSVP",
        "type": "object",
        "description": "Represents a single RSVP response from a guest for a specific event.",
        "properties": {
          "id": { "type": "string", "description": "Unique identifier for the RSVP record." },
          "budgetId": { "type": "string", "description": "The ID of the budget this RSVP belongs to." },
          "guestName": { "type": "string", "description": "The name of the guest responding." },
          "status": { "type": "string", "enum": ["attending", "not_attending"], "description": "The guest's attendance status." },
          "additionalGuests": { "type": "number", "description": "The number of additional guests the person is bringing (+1s)." },
          "respondedAt": { "type": "string", "format": "date-time", "description": "Timestamp of when the RSVP was submitted." }
        },
        "required": ["id", "budgetId", "guestName", "status", "additionalGuests", "respondedAt"]
      }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous",
      "google.com",
      "facebook.com",
      "twitter.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": { "$ref": "#/backend/entities/User" },
          "description": "Collection for storing user profiles.",
          "params": [{ "name": "userId", "description": "The unique identifier for the user." }]
        }
      },
      {
        "path": "/users/{userId}/budgets/{budgetId}",
        "definition": {
          "entityName": "Budget",
          "schema": { "$ref": "#/backend/entities/Budget" },
          "description": "Collection for storing budgets of a user.",
          "params": [
            { "name": "userId", "description": "The unique identifier of the user." },
            { "name": "budgetId", "description": "The unique identifier of the budget." }
          ]
        }
      },
      {
        "path": "/users/{userId}/budgets/{budgetId}/categories/{categoryId}",
        "definition": {
          "entityName": "Category",
          "schema": { "$ref": "#/backend/entities/Category" },
          "description": "Collection for storing categories of a budget.",
          "params": [
            { "name": "userId", "description": "The unique identifier of the user." },
            { "name": "budgetId", "description": "The unique identifier of the budget." },
            { "name": "categoryId", "description": "The unique identifier of the category." }
          ]
        }
      },
      {
        "path": "/users/{userId}/budgets/{budgetId}/categories/{categoryId}/items/{itemId}",
        "definition": {
          "entityName": "Item",
          "schema": { "$ref": "#/backend/entities/Item" },
          "description": "Collection for storing items of a category.",
          "params": [
            { "name": "userId", "description": "The unique identifier of the user." },
            { "name": "budgetId", "description": "The unique identifier of the budget." },
            { "name": "categoryId", "description": "The unique identifier of the category." },
            { "name": "itemId", "description": "The unique identifier of the item." }
          ]
        }
      },
      {
        "path": "/users/{userId}/budgets/{budgetId}/mustDos/{mustDoId}",
        "definition": {
          "entityName": "MustDo",
          "schema": { "$ref": "#/backend/entities/MustDo" },
          "description": "Collection for storing critical must-do tasks for a budget.",
          "params": [
            { "name": "userId", "description": "The unique identifier of the user." },
            { "name": "budgetId", "description": "The unique identifier of the budget." },
            { "name": "mustDoId", "description": "The unique identifier of the must-do item." }
          ]
        }
      },
      {
        "path": "/users/{userId}/budgets/{budgetId}/rsvps/{rsvpId}",
        "definition": {
          "entityName": "RSVP",
          "schema": { "$ref": "#/backend/entities/RSVP" },
          "description": "Collection for storing RSVP responses for a specific budget/event.",
          "params": [
            { "name": "userId", "description": "The unique identifier of the user who owns the budget." },
            { "name": "budgetId", "description": "The unique identifier of the budget." },
            { "name": "rsvpId", "description": "The unique identifier of the RSVP response." }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to ensure authorization independence, clarity, and scalability. It leverages path-based ownership for user-specific data and avoids hierarchical authorization dependencies by denormalizing authorization context where necessary. The structure also supports secure list operations via structural segregation.\\n\\nBudgets, Categories and Items are nested under the `/users/{userId}` collection, ensuring that only the user has access to their own data (Path-Based Ownership).\\n\\nThis structure will need to be implemented with corresponding Firebase Security Rules."
  }
}
